import 'package:flutter/material.dart';

class FortuneWheelScreen extends StatelessWidget {
  const FortuneWheelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Koło fortuny'),
      ),
    );
  }
}
